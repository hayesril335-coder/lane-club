import { useMemo, useState } from 'react'
import './FindAlleyPage.css'

const alleys = [
  { id: 1, name: 'Sunset Lanes', area: 'Downtown Los Angeles', distance: '1.2 mi away', price: 20, lanes: 16, rating: '4.8', reviews: 124, tags: ['Late night', 'Food & drinks'], color: 'sunset' },
  { id: 2, name: 'Highland Bowl', area: 'Highland Park', distance: '3.6 mi away', price: 25, lanes: 24, rating: '4.7', reviews: 98, tags: ['Classic lanes', 'Family friendly'], color: 'highland' },
  { id: 3, name: 'The Spare Room', area: 'Hollywood', distance: '4.1 mi away', price: 30, lanes: 12, rating: '4.9', reviews: 203, tags: ['Premium', 'Cocktails'], color: 'spare' },
  { id: 4, name: 'Pin Point Social', area: 'Koreatown', distance: '5.0 mi away', price: 18, lanes: 10, rating: '4.6', reviews: 76, tags: ['Best value', 'Open late'], color: 'pinpoint' },
]

export default function FindAlleyPage({ onBack, onSelect }) {
  const [query, setQuery] = useState('')
  const [price, setPrice] = useState('Any price')
  const [sort, setSort] = useState('Recommended')
  const [saved, setSaved] = useState([])
  const filtered = useMemo(() => alleys.filter(alley => alley.name.toLowerCase().includes(query.toLowerCase()) && (price === 'Any price' || alley.price <= Number(price.replace('$', '')))), [query, price])
  const toggleSaved = (id) => setSaved(items => items.includes(id) ? items.filter(x => x !== id) : [...items, id])
  return <main className="finder-page"><header className="finder-header"><a className="brand finder-brand" href="#home" onClick={(e) => { e.preventDefault(); onBack() }}><span className="brand-mark"><i /><i /><i /></span>LANE CLUB</a><nav><button className="nav-active">Find an alley</button><button>My reservations</button><button>Account</button></nav><div className="finder-user"><i>MT</i><span>Maya Thompson</span><b>⌄</b></div></header><section className="finder-hero"><div className="finder-hero-inner"><p>MEMBERSHIP STARTS HERE</p><h1>Find your <em>home lane.</em></h1><span>Choose a nearby bowling alley and start reserving up to four hours every week.</span><div className="finder-search"><label>⌕ <input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search by alley name or neighborhood" /></label><button>Search</button></div></div></section><section className="finder-content"><aside className="finder-filters"><div className="filter-title"><h2>Filters</h2><button onClick={() => { setPrice('Any price'); setQuery('') }}>Clear all</button></div><div className="filter-block"><h3>Location</h3><button className="location-button">⌖ Los Angeles, CA <b>⌄</b></button></div><div className="filter-block"><h3>Monthly membership</h3>{['Any price','$20','$25','$30'].map(item => <label key={item}><input type="radio" name="price" checked={price === item} onChange={() => setPrice(item)} /> <span>{item === 'Any price' ? item : `Up to ${item}/month`}</span></label>)}</div><div className="filter-block"><h3>Alley amenities</h3>{['Late night hours','Food & drinks','Family friendly','Private events'].map(item => <label key={item}><input type="checkbox" /> <span>{item}</span></label>)}</div></aside><section className="alley-results"><div className="results-head"><div><strong>{filtered.length} bowling alleys near you</strong><span>Membership includes up to 4 reservation hours/week.</span></div><label>Sort by <select value={sort} onChange={e => setSort(e.target.value)}><option>Recommended</option><option>Lowest price</option><option>Highest rated</option><option>Nearest</option></select></label></div><div className="alley-card-grid">{filtered.map(alley => <article className="alley-card" key={alley.id}><div className={`alley-photo ${alley.color}`}><span>● ● ●</span><button onClick={() => toggleSaved(alley.id)}>{saved.includes(alley.id) ? '♥' : '♡'}</button><small>{alley.distance}</small></div><div className="alley-card-body"><div className="alley-title"><div><h2>{alley.name}</h2><p>⌖ {alley.area}</p></div><span>★ {alley.rating} <i>({alley.reviews})</i></span></div><div className="alley-meta"><b>{alley.lanes} lanes</b>{alley.tags.map(tag => <span key={tag}>{tag}</span>)}</div><div className="alley-price"><div><strong>${alley.price}</strong><span>/ member / month</span></div><button onClick={onSelect}>View alley →</button></div></div></article>)}</div>{filtered.length === 0 && <div className="no-results"><strong>No alleys match that search.</strong><button onClick={() => { setQuery(''); setPrice('Any price') }}>Clear filters</button></div>}</section></section></main>
}

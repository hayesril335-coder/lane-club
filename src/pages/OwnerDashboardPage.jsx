import { useState } from 'react'
import './OwnerDashboardPage.css'

export default function OwnerDashboardPage({ onBack, onLanes, onReservations, onSettings }) {
  const [open, setOpen] = useState(false)
  const go = action => { setOpen(false); action() }
  return <main className="dashboard-page">{open && <aside className="dash-nav owner-menu-panel"><a className="brand dash-brand" href="#home" onClick={event => { event.preventDefault(); go(onBack) }}><span className="brand-mark"><i /><i /><i /></span>LANE CLUB</a><div className="alley-switch"><span>SL</span><div><strong>Sunset Lanes</strong><small>Owner account</small></div></div><nav><button className="selected" onClick={() => setOpen(false)}>Overview</button><button onClick={() => go(onReservations)}>Reservations</button><button onClick={() => go(onLanes)}>Lanes</button><button onClick={() => go(onSettings)}>Settings</button></nav><button className="dash-logout" onClick={() => go(onBack)}>← Log out</button></aside>}<button className="owner-hamburger" onClick={() => setOpen(value => !value)} aria-label="Open owner menu">☰</button><section className="dash-content"><header className="dash-header"><div><p>OWNER OVERVIEW</p><h1>Good morning, <em>Jamie.</em></h1></div><button className="add-reservation" onClick={onReservations}>+ New reservation</button></header><div className="metric-grid"><article><p>ACTIVE MEMBERS</p><strong>148</strong><small>↑ 12% vs. last month</small></article><article><p>MONTHLY REVENUE</p><strong>$2,960</strong><small>↑ 8.4% vs. last month</small></article><article><p>LANE UTILIZATION</p><strong>82%</strong><small>13 of 16 lanes active</small></article><article><p>MEMBER HOURS USED</p><strong>476 hrs</strong><small>of 592 available this month</small></article></div><article className="reservation-card"><div className="card-title"><div><h2>Upcoming reservations</h2><p>Cash and POS walk-ins are managed in Reservations.</p></div><button onClick={onReservations}>Open reservations →</button></div></article></section></main>
}

import { useState } from 'react'
import './MemberSettingsPage.css'

export default function MemberSettingsPage({ onBack }) {
  const [section, setSection] = useState('Profile')
  const [saved, setSaved] = useState('')
  const save = (event, message) => { event.preventDefault(); setSaved(message) }
  return <main className="settings-page"><header className="settings-header"><a className="brand finder-brand" href="#back" onClick={(e) => { e.preventDefault(); onBack() }}><span className="brand-mark"><i /><i /><i /></span>LANE CLUB</a><nav><button onClick={onBack}>My reservations</button><button onClick={onBack}>Find an alley</button><button className="nav-active">Account</button></nav><div className="finder-user"><i>MT</i><span>Maya Thompson</span><b>⌄</b></div></header><section className="settings-shell"><header><p>ACCOUNT SETTINGS</p><h1>Your <em>account.</em></h1><span>Manage your profile, membership, and billing preferences.</span></header><div className="settings-layout"><aside className="settings-nav">{['Profile','Membership','Payment method','Notifications'].map(item => <button key={item} className={section === item ? 'active' : ''} onClick={() => { setSection(item); setSaved('') }}>{item}</button>)}<hr /><button className="danger" onClick={() => setSection('Cancel membership')}>Cancel membership</button></aside><section className="settings-panel">{section === 'Profile' && <form onSubmit={e => save(e, 'Profile saved successfully.')}><div className="settings-heading"><h2>Profile</h2><p>Update your personal information.</p></div><div className="profile-avatar"><b>MT</b><div><strong>Profile photo</strong><span>JPG, PNG, or GIF. Max 5MB.</span><button type="button">Upload a new photo</button></div></div><div className="settings-fields"><label>Full name<input defaultValue="Maya Thompson" required /></label><label>Email address<input type="email" defaultValue="maya.thompson@example.com" required /></label><label>Phone number<input type="tel" defaultValue="(323) 555-0182" /></label></div><button className="settings-save">Save changes</button></form>}{section === 'Membership' && <section><div className="settings-heading"><h2>Membership</h2><p>Your current bowling membership.</p></div><div className="membership-setting-card"><div className="setting-alley"><b>SL</b><div><strong>Sunset Lanes</strong><span>Downtown Los Angeles · Active member</span></div><i>Active</i></div><div className="setting-membership-info"><div><span>MONTHLY PRICE</span><strong>$20 / month</strong></div><div><span>NEXT BILLING DATE</span><strong>July 12, 2026</strong></div><div><span>WEEKLY LANE TIME</span><strong>4 hours</strong></div></div><button onClick={onBack}>View alley details →</button></div></section>}{section === 'Payment method' && <section><div className="settings-heading"><h2>Payment method</h2><p>Your membership renews automatically every month.</p></div><div className="saved-card"><div className="visa-mark">VISA</div><div><strong>Visa ending in 4242</strong><span>Expires 06/28</span></div><button>Edit</button></div><button className="add-payment">+ Add payment method</button></section>}{section === 'Notifications' && <form onSubmit={e => save(e, 'Notification preferences saved.')}><div className="settings-heading"><h2>Notifications</h2><p>Choose what you would like to hear about.</p></div>{[['Reservation reminders','Get a reminder before each lane reservation.'],['Weekly hour reminders','Get a nudge before unused weekly hours reset.'],['Membership updates','Get billing and membership account updates.']].map(([title, description], index) => <label className="notification-row" key={title}><span><strong>{title}</strong><small>{description}</small></span><input type="checkbox" defaultChecked={index < 2} /></label>)}<button className="settings-save">Save preferences</button></form>}{section === 'Cancel membership' && <section><div className="settings-heading"><h2>Cancel membership</h2><p>We are sorry to see you go.</p></div><div className="cancel-box"><strong>Your membership stays active until July 12, 2026.</strong><p>You will still be able to use any remaining reservation hours until then. You can restart at any time.</p><button onClick={() => setSaved('Cancellation request saved. Your membership remains active through July 12, 2026.')}>Cancel my membership</button></div></section>}{saved && <p className="settings-message">{saved}</p>}</section></div></section></main>
}

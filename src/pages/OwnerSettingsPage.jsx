import { useState } from 'react'
import './OwnerSettingsPage.css'
export default function OwnerSettingsPage({ onBack, onLanes }) { const [name, setName] = useState('Sunset Lanes'); const [saved, setSaved] = useState(false); return <main className="owner-settings"><header><button onClick={onBack}>← Back to overview</button><strong>LANE CLUB</strong></header><section><p>ALLEY SETTINGS</p><h1>Manage your <em>alley.</em></h1><form onSubmit={e => { e.preventDefault(); setSaved(true) }}><label>Bowling alley name<input value={name} onChange={e => setName(e.target.value)} /></label><button>Save alley name</button>{saved && <small>Alley name saved.</small>}</form><article><h2>Lane names</h2><p>Rename lanes and manage their availability in lane management.</p><button onClick={onLanes}>Manage lane names →</button></article></section></main> }
